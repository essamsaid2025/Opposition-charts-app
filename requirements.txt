# Streamlit >= 1.42 for native OpenID Connect authentication (st.login/st.user).
streamlit>=1.42
pandas
matplotlib
numpy
openpyxl
mplsoccer
# Optional interactive (Plotly) preview for the comparison charts. The default
# static matplotlib render/export path does not require it.
plotly>=5.0
# Open Play now imports through the fap platform in src/, which needs these.
# Kept in sync with the dependency list in pyproject.toml.
PyYAML
scipy
pillow
# QR-code report blocks (pure-Python; renders to a PIL image, pairs with pillow).
qrcode
# Identity layer: Streamlit's native OIDC auth uses Authlib under the hood.
Authlib>=1.3
# Persistent dataset storage: normalized frames are stored as Parquet
# (dtype-exact, compressed, fastest re-open) so data survives restarts.
pyarrow>=14
# Internal xG Model v1.0 inference: the frozen model (internal-xg) is a
# scikit-learn pipeline loaded via joblib. Required so shot-level internal_xg
# (and Match Stats xG/NPxG) work in deployed runtimes (e.g. Streamlit Cloud),
# not just where the ML stack happens to be installed. Pinned to the version the
# model was trained/saved with to avoid unpickle version mismatch. xgboost is
# NOT needed for inference (training only).
scikit-learn==1.9.0
joblib>=1.3
# --- Phase 11 production infrastructure (OPTIONAL / import-guarded) ---------
# The default sqlite/local/disk stack runs with NONE of these. Uncomment (or
# `pip install .[production]`) when the matching backend is configured:
#   database.backend = 'libsql'  ->  libsql-experimental>=0.0.30   (Turso)
#   storage.backend  = 's3'      ->  boto3>=1.34                   (S3/R2/B2/MinIO)
#   cache.backend    = 'redis'   ->  redis>=5.0
# libsql-experimental>=0.0.30
# boto3>=1.34
# redis>=5.0
