"""Built-in application pages. Importing each module registers its Page with
the page_registry. Kept separate from the legacy fap.ui.pages package.

Discovery (fap.ui.page.load_builtin_pages) imports every module in this
package, so a new page needs no import here.
"""
