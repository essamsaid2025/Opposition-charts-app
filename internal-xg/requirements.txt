# Internal xG model — pinned to versions verified on Python 3.14 (win_amd64).
# Data + numerics
numpy>=2.0
pandas>=2.2
scipy>=1.13
requests>=2.31
# Modelling
scikit-learn>=1.5
xgboost>=2.0
joblib>=1.3
# Evaluation / plots
matplotlib>=3.8
# Optional: faster parquet IO (CSV fallback works without it)
pyarrow>=15.0 ; python_version < "3.14"
# Testing
pytest>=8.0
